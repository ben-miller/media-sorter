## Usage

```
Usage: sort-media.rb [source_dir] [target_dir] [options]
    -b, --batch-size SIZE            Number of files to process in a batch
        --no-dry-run                 Run the sorter without dry run mode
    -h, --help                       Prints this help
```

